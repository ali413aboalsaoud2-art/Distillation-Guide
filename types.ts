export enum AppView {
  CALCULATOR = 'calculator',
  MAINTENANCE = 'maintenance',
  PROCEDURES = 'procedures',
}

export interface AntoineParams {
  name: string;
  A: number;
  B: number;
  C: number;
}